_schema-version: "3.2"
ID: crudoperations
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: crudoperations-destination-content
  type: com.sap.application.content
  requires:
  - name: crudoperations-destination-service
    parameters:
      content-target: true
  - name: crudoperations-repo-host
    parameters:
      service-key:
        name: crudoperations-repo-host-key
  - name: crudoperations-uaa
    parameters:
      service-key:
        name: crudoperations-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: crudoperations_html_repo_host
          ServiceInstanceName: crudoperations-html5-srv
          ServiceKeyName: crudoperations-repo-host-key
          sap.cloud.service: crudoperations
        - Authentication: OAuth2UserTokenExchange
          Name: crudoperations_uaa
          ServiceInstanceName: crudoperations-xsuaa-srv
          ServiceKeyName: crudoperations-uaa-key
          sap.cloud.service: crudoperations
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: crudoperations-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: crudoperations-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - crudoperations.zip
      name: crudoperations
      target-path: resources/
- name: crudoperations
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: crudoperations-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: crudoperations-destination-service
    service-plan: lite
- name: crudoperations-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: crudoperations-xsuaa-srv
    service-plan: application
- name: crudoperations-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: crudoperations-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
